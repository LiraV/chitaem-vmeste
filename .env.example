# Anthropic API key used by the server-side proxy in api/.
# Never prefix this with VITE_ — that would ship it to the browser.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# CLAUDE_MODEL=claude-sonnet-4-6
# PORT=3000
